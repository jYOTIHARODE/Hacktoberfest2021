#include<bits/stdc++.h>
using namespace std;
int main() {
    string str1,str2;
    cin>>str1>>str2;

    str1 = str1+str2;
    str2 = str1.substr(0,(str1.length() - str2.length()));
    str1 = str1.substr(str2.length());
    cout<<str1 << " "<<str2;00
}
